#include <stdio.h>
#include <stdlib.h>
#define ll long long

ll fib(int n)
{
    in
    if(n == 0) 
        return 0;
    if(n == 1)
        return 0;
    while(n--)
    {

    }
}
int main()
{
    int n, value;
    printf("Nhap vao n: ");
    scanf("%d", &n);
    printf("gia tri cua so thu n trong day fibonacci la: %d", value);
    return 0;
}