#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define ll long long

int main()
{
    for(int i = 1; i <= 20; i++)
    {
        if(i % 2 == 0)
            printf("%d  ", i);
    }
    return 0;
}
